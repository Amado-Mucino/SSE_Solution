namespace SSE.Entidades.Modelos
{
    public class Rol
    {
        // Identificador del rol (INT, PK, AI) 
        public int IdRol { get; set; }

        // Nombre del rol (Administrador, etc.) 
        public string NombreRol { get; set; }
    }
}